# Nutanix v4 Go Shell Samples

For usage instructions, see the `README.md` file in each code sample directory.

## Requirements

To run the shell code samples, ensure the following requirements are met:

1. [Install jq](https://jqlang.org/doc/install)
2. `curl` command must be available
3. `uuidgen` command must be available